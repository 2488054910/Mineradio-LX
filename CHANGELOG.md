# 更新日志

本项目的所有重要变更都记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [2.1.3] - 2026-09-06

### 修复（免费音源不可用问题）

第三方音源生态在 2026 年 8-9 月发生多处变更，导致免费音源大面积失效。本版本逐一跟进修复，并增强了多级兜底：

- **音质回退映射修复**：`128k` 回退档此前被错误映射为 `flac24bit`（qualityMap 无 `128k` 键时回落到 `hires`），低音质回退从未真正生效；现在原生音质键直接透传
- **URL 校验修复**：部分后端（如 huibq）在返回真实可播链接的同时携带"无法获取播放链接"的模板文案，旧校验器会误杀这些链接；现在以 URL 本身为准，仅拒绝空链接和非 http(s) 内容
- **ChKSz 音源迁移**：`api.chksz.top` 已正式弃用，迁移到 `api.chksz.com`；同时修复该后端此前对所有平台都请求网易云专用接口（`/api/163_music`）的问题，现按来源分发（`163_music` / `qq_music` / `kugou_music`），支持 `apikey` 查询参数鉴权（新域名需注册密钥）
- **GD Studio 搜索兜底强化**：
  - 匹配阈值从"仅歌名相似（≥30 分）"提高到"歌名精确匹配 + 歌手重合"，杜绝把翻唱/翻唱版当原唱播放
  - 新增繁简体归一化（`周杰倫`↔`周杰伦`），修复 joox/kuwo 搜索结果因繁简差异匹配失败
  - QQ 歌曲直查 joox 必然失败（两套 ID 体系）的问题由"搜索换 ID"路径承接，实测可解析
- **新增跨源救援（cross-source rescue）**：全部后端对原平台失败后，自动通过 GD Studio 在酷我/joox 搜索同一首歌（严格同名+同歌手匹配），再交由支持该平台的后端（含星海酷我源）解析，带 `_noRescue` 防递归
- **节流与重试**：前端 `nocache=1` 重试现在同时绕过 1.5 秒最小间隔节流窗，修复第二次尝试被 `throttled:min_gap` 全量拒绝的问题；服务端 QQ 老接口跨源搜索的 `interval` 秒/毫秒单位换算错误一并修复
- **后端码位补全**：`401`（ChKSz 新域名未带密钥）、`500`（星海外部服务异常）纳入明确拒绝码，状态面板可显示真实失败原因

### 变更

- 默认/本地音源配置：ChKSz 域名更新为 `https://api.chksz.com`；重新启用 huibq 后端（旧版因 URL 校验误判其失效）
- 版本号 2.1.2 → 2.1.3

### 验证

- 冒烟测试 T1-T11 全部通过（新增 T9 URL 构造 / T10 跨源救援 / T11 节流旁路用例）
- 真实后端实测（周杰伦《晴天》）：网易云 VIP 版经 huibq 解析成功（320k，音频流验证 HTTP 206）；QQ 版经 GD Studio 搜索兜底在 joox 解析成功（320k）

## [2.1.2] - 2026-08-11

### 修复

- **修复 v2.1.1 安装包启动崩溃**：打包白名单（`build.files`）缺少 `lxmusic-debug-log.js` 和 `data/` 配置目录，导致 `server.js` 加载模块失败、程序无法启动
- 补充打包配置：`data/**/*`（默认音源配置）、`lxmusic-debug-log.js` 加入打包白名单
- 仓库源码同步完整构建配置（`scripts` / `build` / `devDependencies`），clone 后可正常 `npm run build:win`

### 变更

- 版本号 2.1.1 → 2.1.2（修复发布，v2.1.1 安装包已标记废弃）

## [2.1.1] - 2026-08-11

### 🎉 首个二创发布

基于原版 [XxHuberrr/Mineradio v2.1.0](https://github.com/XxHuberrr/Mineradio/releases/tag/v2.1.0) 二次开发。

### 新增

- 落雪音乐（LX Music）兼容音源接入：播放地址可优先从 LX 兼容音源获取
- 多音源管理：星海 / ChKSz / ikun / huibq / 野花等后端自由切换、启停
- 音质选择：标准 128k / 高品 320k / 无损 FLAC / Hi-Res 24bit
- 内置霞鹜文楷歌词字体（Light / Regular / Medium 三字重）
- 独立更新通道：软件内更新检测指向本仓库

### 变更

- 版本号从 2.1.0 提升至 2.1.1，与原版区分
- 更新通道、发布配置指向 `2488054910/Mineradio-LX`

### 保留（原版 v2.1.0 全部功能）

- 天气电台、每日推荐、私人电台、继续听、听歌画像、我的歌单
- 粒子视觉（emily / 安魂 / 星河 / 唱片 / 星球 / 滚筒 / 虚空 / 泡沫 / 不规则 / 棋盘）
- 电影镜头系统、歌词舞台、桌面歌词、壁纸模式
- 3D 歌单架、手势控制、用户存档
- 网易云 / QQ 音乐登录与多平台搜索
